1.特别注意
	1)	当新建数据库时，请向启用数据库的uuid_genarate系列函数

2.python环境配置
	mkvirtualenv -p /usr/bin/python3 cmapi-py3
		pip
		setuptools
	
	pip install psycopg2
		psycopg2
	
	pip install sqlalchemy
		SQLAlchemy
	
	pip install flask
		Flask
		Werkzeug
		Jinja2
		itsdangerous
		MarkupSafe
	
	pip install flask-restful
		Flask-RESTful
		aniso8601
		six
		pytz
	
	pip install flask-sqlalchemy
		Flask-SQLAlchemy
	
	pip install flask-cors
		Flask-Cors
	
	pip install alembic
		alembic
		Mako

	pip install passlib
		passlib

	pip install flask-httpauth
		Flask-HTTPAuth

	pip install pyld
		pyLD	
			
	
